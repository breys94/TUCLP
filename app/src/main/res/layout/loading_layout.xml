<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center_vertical"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/load_logo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="10dp"
        android:src="@drawable/amibo400p400"/>

    <TextView
        android:id="@+id/load_text"
        android:layout_marginTop="270dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/load_logo"
        android:text="@string/loading"
        android:textStyle="bold"
        android:gravity="center"
        android:textColor="#222222"
        android:textSize="30dp"/>

    <ProgressBar
        android:id="@+id/load_pb"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/load_text"
        android:progressTint="#DD0000"
        style="?android:attr/progressBarStyleHorizontal"
        android:max="100"
        android:progress="50"
        android:layout_marginLeft="30dp"
        android:layout_marginRight="30dp"
        android:layout_marginTop="50dp"
        />

    <TextView
        android:id="@+id/load_text_info"
        android:layout_marginTop="10dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/load_pb"
        android:text="@string/text_info_load"
        android:textStyle="bold"
        android:gravity="center"
        android:textColor="#222222"
        android:textSize="18dp"/>

</RelativeLayout>